accessing
embeddingSize
	"The number of dimensions in each embedding. Higher values improve precision but can impact computing efficiency (memory consumption, search speed). Only supported in some models."

	^ self argumentAt: #embeddingSize